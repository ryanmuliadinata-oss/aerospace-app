server.port=8080
aerospace.api.flightaware.key=${FLIGHTAWARE_API_KEY:}
# Set AEROSPACE_API_KEY in the environment to enable API key auth on all non-health endpoints.
# Leave unset (or empty) to disable auth — useful for local development.
aerospace.api.key=${AEROSPACE_API_KEY:}
